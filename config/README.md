# config

本目录存放本地工具和外部软件适配器的配置文件。

## 当前主要内容

| 文件 | 作用 |
| --- | --- |
| `ansys_config.json` | ANSYS / PyMAPDL 相关检测结果和执行配置。 |

## 维护规则

- 配置文件可以手动校正，但不要写入 API Key、密码或 token。
- 模型 API 配置属于 `.env`，不要放在本目录。
- 修改外部软件路径后，建议运行对应检查脚本验证。
